// URL base de la PokéAPI
export const BASE_URL = 'https://pokeapi.co/api/v2'

// Paginación
export const DEFAULT_PAGE_SIZE = 20
export const PAGE_SIZE_OPTIONS = [10, 20, 50]

// Tipos no jugables que no se muestran en el filtro
export const EXCLUDED_TYPES = ['unknown', 'stellar', 'shadow']

// Valor máximo de una estadística base (para el ancho de las barras)
export const MAX_STAT = 255